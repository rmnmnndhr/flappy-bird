// #pragma once

// #include <SDL3/SDL.h>

// #include <string>
// #include <unordered_map>

// class TextureManager {
// public:
//     // Loads a texture and stores it with a key (name)
//     bool load(const std::string& key, const std::string& filepath, SDL_Renderer* renderer);

//     // Retrieves a texture by key
//     SDL_Texture* get(const std::string& key) const;

//     // Frees all loaded textures
//     void clear();

// private:
//     std::unordered_map<std::string, SDL_Texture*> textures;
// };
